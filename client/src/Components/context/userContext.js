import { createContext } from "react";

// Create a new context called userContext
const userContext = createContext();

// Export the userContext so other components can use it
export default userContext;
