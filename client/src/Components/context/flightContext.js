import { createContext } from "react";

// Create a new context called flightContext
const flightContext = createContext();

// Export the flightContext so other components can use it
export default flightContext;
